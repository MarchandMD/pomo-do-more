<!DOCTYPE html>
<html>
  <head>
    <title><%= yield(:title) || "Pomo-do-more" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <div class="d-flex flex-column">
      <div class="row flex-grow-1 p-3">
        <div class="col d-flex">
          <div class="flex-grow-1 text-center">pomo-do-more</div>
        </div>
        <div class="d-flex justify-content-end px-5 py-2 d-none bg-dark text-white">
          why?
        </div>
      </div>
      <div class="row p-3">
        <div class="col">
        </div>
      </div>
      <%= yield %>
    </body>
  </div>
</html>
