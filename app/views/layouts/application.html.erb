<!DOCTYPE html>
<html>
  <head>
    <title><%= yield(:title) || "Pom + Music" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <div class="d-flex flex-column">
      <div class="row flex-grow-1 p-3">
        <div class="col d-flex">
          <div class="flex-grow-1 text-center">
            <h1>Pom + Music</h1>
            <span>Pomodoro timer with music</span>
          </div>
        </div>
        <div class="d-flex justify-content-end px-5 py-2 bg-dark text-white">
          <div class="">
            <%= link_to "why?", root_path, class: "d-none text-decoration-none text-white" %>
          </div>
        </div>
      </div>
      <div class="row p-3">
        <div class="col">
        </div>
      </div>
      <%= yield %>
    </body>
  </div>
</html>
